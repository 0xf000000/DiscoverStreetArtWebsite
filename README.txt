--- um die Application zu starten --- 

-> mysql, mvn should be installed

To run the application a database user should be configured
-> SSL (https) should be disabled and you can enabled in application.properties

U find the configurations in : ~/backend/src/main/resources/application.properties

### START ## 

cd ~/backend/

mvn spring-boot:run

